<h1>THIS IS THE FLOOR PLAN INDEX</h1>

<div class="row">
  <% @floor_plans.each do |fp| %>
    <div class="col-md-4">
      <div class="card">
        <%#= image_tag portfolio_img(portfolio_item.thumb_image, 'thumb'), width: '100%'%>
          <div>
            <%= link_to image_tag(fp.main_img), floor_plan_path(fp) %>
          </div>
      </div>
    </div>
  <% end%>
</div>
